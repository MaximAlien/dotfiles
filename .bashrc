alias ll="ls -la"
alias cd.='cd ..'
alias cd..='cd ..'

# Colors
BLUE_COLOR='\033[0;34m'
NO_COLOR='\033[m'

bashrc_loading() {
    echo -e "${BLUE_COLOR}Starting ${SHELL}...${NO_COLOR}"
}

bashrc_loading
